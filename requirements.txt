# Core dependencies for Ayrshare MCP Server
fastmcp>=2.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Optional development dependencies
# Install with: pip install -r requirements.txt -r requirements-dev.txt
